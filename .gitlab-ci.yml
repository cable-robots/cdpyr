image: kiwicom/tox


stages:
    - test

before_script:
    - curl -O https://bootstrap.pypa.io/get-pip.py
    - python get-pip.py
    - pip install -r dev-requirements.txt

python37:
    stage: test
    script: tox -e py37

check:
    stage: test
    script: tox -e check

spell:
    stage: test
    script: tox -e spell

docs:
    stage: test
    script: tox -e docs

report:
    stage: test
    script: tox -e dogfood
